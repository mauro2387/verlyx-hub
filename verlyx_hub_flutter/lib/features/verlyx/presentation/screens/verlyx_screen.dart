import 'package:flutter/material.dart';

class VerlyxScreen extends StatelessWidget {
  const VerlyxScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Verlyx Ecosystem')),
      body: const Center(child: Text('Verlyx Module - Coming Soon')),
    );
  }
}
